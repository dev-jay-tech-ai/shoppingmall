import { Outlet } from "react-router-dom"

const AboutPage = () => {
    return (

        <div>
            About Page
            <Outlet></Outlet>
        </div>
        
    )
}

export default AboutPage